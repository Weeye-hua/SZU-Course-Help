## 无需 Python，完整解压后即可使用

本版本提供 Windows、macOS 和 Linux 原生发布包。普通用户请下载与自己系统匹配的 ZIP，不要下载 GitHub 自动附带的 Source code 压缩包。

## v3.6.4 更新说明

1. 修复学校提示“添加选课志愿成功”但课程没有真正进入已选列表时，本地仍误报成功的问题。
2. 成功或“已经选过”现在只作为候选结果；程序最多回查 3 次学校已选课程，并且只有完整教学班 ID 精确匹配才写入成功。
3. 已选列表有效但目标教学班尚未出现时，课程保持活动并继续尝试；不会用课程名称、教师或上课时间模糊确认。
4. 核验时会话过期会进入原有 OCR 自动重登录；核验接口连续不可用、响应畸形、缺少必要字段或本地状态无法保存时保护性暂停，并在网页显示原因。
5. 审查、补强并合入 PR #13：隐藏冲突或满额后没有可见教学班的课程组会整体隐藏，搜索匹配数、分页和普通页面可见数量同步更新，已选教学班始终保留。
6. 保留爆发/一般模式自定义失败次数与无限次选项、2000 次未知响应保护、暂停编辑、安全停止、课表、学分统计、多校区和账号隔离缓存。
7. OCR 自动重登录继续兼容 `ddddocr 1.6.1` 多种 API；学校密码只保存在当前进程内存中，WebVPN 仍只用于认证后的只读查询。
8. 自动测试全部使用假数据并拦截未模拟的外部请求，不访问学校，也不执行选课或退课操作。
9. Release 继续提供 Windows、macOS、Linux 与源码 ZIP，不附带独立 `.sha256` 或 `SHA256SUMS.txt` 文件。

| 系统 | 下载文件 | 启动方式 |
| --- | --- | --- |
| Windows 10/11 64 位 | `SZU-Course-Help-v3.6.4-windows-x64.zip` | 双击 `启动抢课助手.bat` |
| Apple 芯片 Mac | `SZU-Course-Help-v3.6.4-macos-arm64.zip` | 双击 `启动抢课助手.command` |
| Intel 芯片 Mac | `SZU-Course-Help-v3.6.4-macos-x64.zip` | 双击 `启动抢课助手.command` |
| Linux 64 位 | `SZU-Course-Help-v3.6.4-linux-x64.zip` | 运行 `启动抢课助手.sh` |

每个发布包均含可执行程序、平台启动脚本、`使用手册.md`、`使用手册.pdf`、`更新记录.md`、项目说明和许可证。`SZU-Course-Help-v3.6.4-source.zip` 供开发者使用。

## 首次运行

1. 完整解压 ZIP，不要在压缩包预览窗口内直接运行，也不要只拖出主程序。
2. 运行平台启动脚本，在终端输入学号并生成本机 Card Key。
3. 输入 `Y` 后，程序会启动本地网页并打开登录页。
4. 首次登录由用户输入学校密码并手动完成点击验证码。
5. 预选、未开放、已结束和未知阶段禁止启动自动选课；只有学校返回明确允许的复选、正选、补选或补退选批次才可在二次确认后启动。

## 数据与安全

- Windows 数据目录：`%APPDATA%\SZU-Course-Help\`。
- macOS 数据目录：`~/Library/Application Support/SZU-Course-Help/`。
- Linux 数据目录：`${XDG_DATA_HOME:-~/.local/share}/SZU-Course-Help/`。
- 学校密码、token 与 Cookie 不写入磁盘；关闭程序后必须重新手动登录。
- 当前版本未购买 Windows 或 Apple 商业代码签名证书，SmartScreen 或 Gatekeeper 可能显示未知开发者提示。
- 只从本仓库官方 Release 下载，不要运行群文件、网盘或陌生链接中的副本。
- 学校接口和规则可能变化，关键选课结果以学校官方系统为准。

完整步骤、任务暂停编辑、数据迁移、自动重登录和常见问题请阅读发布包中的 `使用手册.pdf`。
